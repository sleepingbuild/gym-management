name: CI

on:
push:
branches:
- main

jobs:
test:
runs-on: ubuntu-latest

```
steps:
  - uses: actions/checkout@v4

  - name: Setup Node
    uses: actions/setup-node@v4
    with:
      node-version: 22

  - name: Done
    run: echo "CI works"
```
